# План разработки Terraform/OpenTofu-провайдера для 3x-ui

## 1. Исследование и подготовка
- [x] 1.1 Изучить репозиторий MHSanaei/3x-ui (https://github.com/MHSanaei/3x-ui): доступные REST/GraphQL эндпоинты, форматы запросов/ответов.
- [x] 1.2 Зафиксировать требования к аутентификации (API-ключ, cookie, токен панели) и ограничения безопасности.
- [x] 1.3 Создать документ `docs/api.md` с описанием обнаруженных эндпоинтов и примеров полезных нагрузок.
- [x] 1.4 Собрать минимальную dev-среду: docker-контейнер или локальный стенд 3x-ui для тестов.

## 2. Архитектура провайдера
- [x] 2.1 Определить конфигурацию провайдера: `base_url`, `api_key`, опции TLS/timeout, проверка соединения.
- [x] 2.2 Спроектировать API-клиент на Go (пакет `internal/client`): управление HTTP, ретраи, логирование, декодирование ошибок.
- [x] 2.3 Согласовать список ресурсов и дата-сорсов:
  - [x] `3xui_inbound`, `3xui_user`, `3xui_subscription`, `3xui_panel_setting`, `3xui_cron_job`.
  - [x] `3xui_inbounds`, `3xui_user`, `3xui_usage_stats`, `3xui_raw`.
- [x] 2.4 Схемы Terraform должны точно отражать JSON 3x-ui, сложные поля описать через вложенные блоки/структуры.

## 3. Инфраструктура репозитория
- [x] 3.1 `go mod init github.com/batonogov/terraform-provider-3x-ui` и добавление зависимостей (`terraform-plugin-framework`, `terraform-plugin-log`, `golangci-lint`).
- [x] 3.2 Настроить `Taskfile`: `task lint`, `task test`, `task build`, `task fmt`.
- [ ] 3.3 Подключить CI (GitHub Actions): линт, юнит-тесты, опционально acceptance-тесты (условно при наличии стенда).
- [x] 3.4 Сгенерировать boilerplate через `tfplugindocs` и добавить шаблоны в `docs/` и `examples/`.

## 4. Реализация
- [x] 4.1 Скелет провайдера: функции `Metadata`, `Schema`, `Configure`, валидация конфигурации и проверка подключения.
- [x] 4.2 Реализация API-клиента: типы запросов/ответов, контекст, обработка статусов, перехват rate-limit, unit-тесты с `httptest.Server`.
- [ ] 4.3 CRUD-циклы ресурсов (по приоритету):
  - [x] `3xui_inbound` (базовый функционал).
  - [x] `3xui_user` (привязка к inbound, квоты).
  - [ ] `3xui_subscription` и `3xui_panel_setting`.
  - [ ] `3xui_cron_job`.
- [x] 4.4 Импорт существующих сущностей (`ImportState`), нормализация полей, кастомные валидаторы (например, диапазоны портов).
- [x] 4.5 Реализация дата-сорсов с поддержкой фильтров и агрегации статистики.

## 5. Тестирование
- [x] 5.1 Юнит-тесты для схем, мапперов и клиента (coverage ключевых веток >80%).
- [x] 5.2 Acceptance-тесты `TF_ACC=1` против локального 3x-ui (можно через docker-compose), отдельный workflow.
- [ ] 5.3 Smoke-тест примерных конфигураций из `examples/` перед релизом.

## 6. Документация и релиз
- [ ] 6.1 Обновлять пользовательские гайды (`docs/index.md`, `docs/resources/*.md`, `docs/data-sources/*.md`).
- [ ] 6.2 Добавить примеры использования (создание inbound, привязка пользователей, сбор статистики) в `examples/`.
- [ ] 6.3 Настроить `goreleaser` для публикации бинарей (darwin/linux/windows, amd64/arm64) + подписи.
- [ ] 6.4 Подготовить релиз в Terraform Registry или приватное распространение: тегирование, changelog, инструкции по установке.
